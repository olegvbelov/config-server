server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://beloll@bitbucket.org/belovsteam/configs.git
          username: beloll
          password: iGMJn1RZ
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
