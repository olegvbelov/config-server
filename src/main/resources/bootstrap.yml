server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///var/localconfig
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
